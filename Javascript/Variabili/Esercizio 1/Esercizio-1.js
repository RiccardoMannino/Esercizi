function canPlay() {
    let personName = 'Paul'; // si dichiara la variabile prima della condizione
    if (true) {}
  
    personName += ' plays football'; // Argh! personName is not defined
  
    console.log(personName);
  }
  
  canPlay();